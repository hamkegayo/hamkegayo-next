/**
 * 환불 안내 문구 — 취소 모달과 취소·환불 정책 페이지가 함께 읽는다.
 *
 *  ⚠️ 두 곳에 따로 적으면 반드시 어긋난다. 특히 소요일은 **PG 실계약 후
 *     카드사 기준으로 바뀔 수 있는 값**이라(#81), 그때 한쪽만 고치면
 *     고객이 보는 안내가 화면마다 달라진다.
 *
 *  금액 계산은 여기 없다. 그것은 `lib/payments/refund.ts` 가 하고, 화면은
 *  그 결과를 그대로 보여준다(#76).
 */

/** 카드사 일반값. PG 실계약 후 실제 기준으로 확정한다(#81). */
export const REFUND_BUSINESS_DAYS = "3~5일";

/** 환불 수단·소요일 */
export const REFUND_METHOD_NOTE = `환불은 결제하신 수단으로 처리되며, 카드사에 따라 영업일 기준 ${REFUND_BUSINESS_DAYS}이 걸릴 수 있습니다.`;

/** 포인트 처리 — 약관 제19조의 취소수수료는 현금 결제분에서만 뗀다 */
export const POINT_RESTORE_NOTE =
    "사용하신 포인트는 취소수수료와 무관하게 전액 복원됩니다.";
